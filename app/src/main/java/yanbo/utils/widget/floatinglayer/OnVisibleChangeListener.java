package yanbo.utils.widget.floatinglayer;

/**
 * @author Cuizhen
 */
public interface OnVisibleChangeListener {
    void onShow(FloatingLayer popupView);

    void onDismiss(FloatingLayer popupView);
}
